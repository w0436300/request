// Copy to auth-config.js for local preview, then set a SHA-256 hex hash of your password.
// Do not commit auth-config.js. CI generates it from the SITE_PASSWORD secret at deploy time.
window.__SITE_PASSWORD_HASH__ = "REPLACE_WITH_SHA256_HEX";
